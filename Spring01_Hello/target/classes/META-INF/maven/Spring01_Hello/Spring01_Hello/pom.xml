<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>
  <groupId>Spring01_Hello</groupId>
  <artifactId>Spring01_Hello</artifactId>
  <version>0.0.1-SNAPSHOT</version>
  <build>
    <sourceDirectory>src</sourceDirectory>
    <plugins>
      <plugin>
        <artifactId>maven-compiler-plugin</artifactId>
        <version>3.8.1</version>
        <configuration>
          <release>11</release>
        </configuration>
      </plugin>
    </plugins>
  </build>
  
  <dependencies>
  <!-- Maven 이용해 라이브러리 추가. Spring도 일종의 라이브러리. -->
  <!-- STS는 일종의 이클립스 기반 툴일 뿐이다. STS != Spring -->
	<dependency>
	    <groupId>org.springframework</groupId>
	    <artifactId>spring-core</artifactId>
	    <version>5.3.24</version>
	</dependency>
	
	<dependency>
	    <groupId>org.springframework</groupId>
	    <artifactId>spring-context</artifactId>
	    <version>5.3.24</version>
	</dependency>
	<!-- 그룹 id가 동일하면 동일한 버전 사용하는 것이 좋다. 안그러면 에러날 확률 매우 높다. -->
  </dependencies>
  <!-- dependencies 설정은 project 태그 내에서 해야한다. -->
</project>